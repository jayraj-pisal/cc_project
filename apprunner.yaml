version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - cd backend && npm ci --omit=dev
run:
  runtime-version: 18
  command: node backend/server.js
  network:
    port: 3001
    env: PORT
  env:
    - name: NODE_ENV
      value: production
